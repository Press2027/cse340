<h1>Add Inventory Item</h1>

<% if (message) { %>
  <p class="notice <%= message.includes('Failed') ? 'error' : 'success' %>"><%= message %></p>
<% } %>

<form action="/inv/add-inventory" method="POST">
  <h2>Add New Vehicle</h2>

  <label for="classification">Classification</label>
  <%- classificationSelect %>

  <label for="inv_make">Make</label>
  <input type="text" name="inv_make" id="inv_make" required value="<%= inv_make || '' %>">

  <label for="inv_model">Model</label>
  <input type="text" name="inv_model" id="inv_model" required value="<%= inv_model || '' %>">

  <label for="inv_year">Year</label>
  <input type="number" name="inv_year" id="inv_year" required value="<%= inv_year || '' %>">

  <label for="inv_description">Description</label>
  <textarea name="inv_description" id="inv_description" rows="4"><%= inv_description || '' %></textarea>

  <label for="inv_image">Image Path</label>
  <input type="text" name="inv_image" id="inv_image" value="<%= inv_image || '/images/no-image.png' %>">

  <label for="inv_thumbnail">Thumbnail Path</label>
  <input type="text" name="inv_thumbnail" id="inv_thumbnail" value="<%= inv_thumbnail || '/images/no-image-available.png' %>">

  <label for="inv_price">Price</label>
  <input type="number" name="inv_price" id="inv_price" step="0.01" required value="<%= inv_price || '' %>">

  <label for="inv_miles">Miles</label>
  <input type="number" name="inv_miles" id="inv_miles" required value="<%= inv_miles || '' %>">

  <label for="inv_color">Color</label>
  <input type="text" name="inv_color" id="inv_color" required value="<%= inv_color || '' %>">

  <button type="submit">Add Vehicle</button>
</form>
